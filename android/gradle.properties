kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true

# Required for the AndroidX (Jetpack Compose) dependencies in :aksara-compose.
android.useAndroidX=true

# Maven coordinates (shared by all published modules). io.github.<user> is the
# GitHub-verifiable namespace on the Sonatype Central Portal.
GROUP=io.github.iqbaladr
VERSION_NAME=0.2.0
